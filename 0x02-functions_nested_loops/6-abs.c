#include "main.h"

/**
 * _abs - Function prototype
 * Description: computes absolute value of integer
 * @n: number to find absolute value
 * Return: absolute value of a number
 */

int _abs(int)
{
	int j = n >= 0 ? n : n * - 1;

	return (j);
}
