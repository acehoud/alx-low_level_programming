#include <ctype.h>
#include "main.h"

/**
 * int _islower - Prototype function
 * Description: This checks between lower case
 * Returns: 1
 */

int _islower(int c);

int main(void)
{
	int check = _islower(c);
}

int _islower(int c)
{
	if (c.islower)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
