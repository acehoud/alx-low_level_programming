#include "main.h"

/**
 * _isdigit - function that checks for a digit (0 through 9).
 * Return: 1 if c is digit otherwise 0
 * @c - we checking c
 */

int _isdigit(int c)
{
	int c = 0;

	while (c <= 9)
	{
		return 1;
		c++;
	}
}
