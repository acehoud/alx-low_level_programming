#include "main.h"

/**
 * _isdigit - function that checks for a digit (0 through 9).
 * Return: 1 if c is digit otherwise 0
 * @c - we checking c
 */

int _isdigit(int c)
{
	char c = 'a'
	char c = 'A';
	c = 0;

	while (c <= 9)
	{
		while (c <= 'z')
		{
			return 0;
		}
		while (c <= 'Z')
		{
			return 0;
		}

		return 1;
		c++;
	}
}
