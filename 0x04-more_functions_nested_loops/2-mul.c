#include "main.h"

/**
 * mul- function that multiplies two integers
 * Description: we multiply to integers
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: product of a and b
 */

int mul(int a, int b)
{
	int i = a * b;

	return (i);
}
