#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * Return: 1 if c is upper, otherwise 0
 * @c: we checking c
 */

int _isupper(int c)
{
	if ((c > 99) && (c > 68))
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0)
}
