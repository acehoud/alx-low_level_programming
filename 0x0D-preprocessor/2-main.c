#include <stdio.h>

/**
 * main - Entry point
 * description: prints the name of the
 * file it was compiled from
 * Return: Always return 0
 */

int main(void)
{
	printf("%s\n", _FILE_);
	return (0);
}
